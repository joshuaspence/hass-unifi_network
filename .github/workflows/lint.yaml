---
name: 'Lint'
on:
  pull_request: {}
  push:
    branches:
      - 'master'
  schedule:
    - cron: '0 0 * * *'
jobs:
  black:
    runs-on: 'ubuntu-latest'
    steps:
      - uses: 'actions/checkout@v3'
      - uses: 'actions/setup-python@v1'
        with:
          python-version: '3.x'
      - run: 'pip install --constraint requirements.txt black'
      - run: 'black --check .'
